{
  "name": "neurostars-games-tracker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run test:unit && npm run test:rules",
    "test:unit": "node --test tests/result-display.test.mjs tests/game-reporter.test.mjs",
    "test:rules": "firebase emulators:exec --only firestore \"node --test tests/firestore-rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.0",
    "firebase": "12.2.1",
    "firebase-tools": "14.15.2"
  }
}
